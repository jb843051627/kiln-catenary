# Kiln Catenary

Kiln Catenary coordinates industrial ceramic kiln thermal profiles and firing runs. It is a single-process Go service backed by a local SQLite file.

Run `GOTOOLCHAIN=local go run . --smoke-test` for a startup probe or `GOTOOLCHAIN=local go run .` for the field HTTP service.
